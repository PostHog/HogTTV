# HogTTV — Claude Code context

## What this repo is

A Chrome extension that renders custom Slack emojis in Google Meet chat.

## Tests

```sh
npm install        # first time only — installs jsdom
node test/emoji.js # exit 0 = pass, non-zero = fail
```
